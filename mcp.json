{
  "mcpServers": {
    "agentmemory": {
      "command": "npx",
      "args": ["-y", "@agentmemory/agentmemory"],
      "description": "Universal memory for all AI tools - 43 MCP tools, 92% token reduction"
    },
    "basic-memory": {
      "command": "uvx",
      "args": ["basic-memory", "mcp"],
      "description": "Human-readable knowledge graph stored as Markdown files"
    },
    "hindsight": {
      "command": "hindsight-server",
      "env": {
        "HINDSIGHT_API_LLM_PROVIDER": "ollama"
      },
      "description": "Knowledge graph with semantic search - learns from your work"
    },
    "repomix": {
      "command": "repomix",
      "args": ["--mcp"],
      "description": "Full codebase context packaging - 80% token reduction"
    },
    "mempalace": {
      "command": "mempalace",
      "args": ["mcp"],
      "description": "Conversation history memory - 96.6% recall accuracy (Claude Code only)"
    }
  }
}
